import { Button, LoginForm } from "ui";
import { Typography, } from  '@mui/material';

const Web = () => {
  return (
    <div>
      <Button />
      <LoginForm />
    </div>
  );
}

export default Web; 
